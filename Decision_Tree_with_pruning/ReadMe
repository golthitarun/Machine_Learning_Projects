We used Java language for coding

To Compile form command line
	javac *.java

To run from command line(make sure that all the data and the class files are in the same folder or give the exact path of the data sets)
	java DecisionTree training_set.csv validation_set.csv test_set.csv 0.2
